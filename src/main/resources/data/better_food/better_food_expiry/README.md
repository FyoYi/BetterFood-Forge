# 食物保质期与新鲜奖励配置说明

## 📁 文件位置
`data/better_food/better_food_expiry/` 目录下的所有 JSON 文件都会被加载。

## 📝 JSON 格式说明

### 基础结构
```json
{
  "ticks": 120000,
  "items": [
    // 物品配置...
  ]
}
```

- **ticks**: 保质期时长（游戏刻，20 ticks = 1秒）
  - `-1` = 永久保鲜（不会腐烂）
  - 示例：`120000` = 6000秒 = 100分钟

### 物品配置格式

#### 方式1：简单格式（仅配置保质期）
只需要物品ID字符串，不配置新鲜奖励效果：
```json
{
  "ticks": 120000,
  "items": [
    "minecraft:cookie",
    "minecraft:dried_kelp"
  ]
}
```

#### 方式2：完整格式（保质期 + 新鲜奖励）
使用对象格式，可以配置物品的新鲜奖励效果：
```json
{
  "ticks": 120000,
  "items": [
    {
      "item": "minecraft:cooked_beef",
      "effects": [
        {
          "effect": "saturation",
          "chance": 0.8,
          "duration": 5,
          "amplifier": 0
        },
        {
          "effect": "regeneration",
          "chance": 0.3,
          "duration": 3,
          "amplifier": 0
        }
      ]
    }
  ]
}
```

#### 方式3：混合格式
可以在同一个文件中混合使用两种格式：
```json
{
  "ticks": 120000,
  "items": [
    "minecraft:cookie",
    {
      "item": "minecraft:apple",
      "effects": [
        {
          "effect": "saturation",
          "chance": 0.6,
          "duration": 3,
          "amplifier": 0
        }
      ]
    },
    "minecraft:dried_kelp"
  ]
}
```

## 🎯 效果参数说明

### effect (必填)
效果类型，**只支持以下 6 种食物奖励效果**（中英文都可以）：

| 英文名 | 中文名 | 说明 |
|--------|--------|------|
| `saturation` | 饱和 | 快速恢复饱食度 |
| `regeneration` | 生命恢复 | 持续恢复生命值 |
| `absorption` | 伤害吸收 | 额外的金色心（吸收伤害） |
| `fire_resistance` | 防火 | 免疫火焰伤害 |
| `water_breathing` | 水下呼吸 | 水下不消耗氧气 |
| `luck` | 幸运 | 提高掉落物品质量 |

⚠️ **注意**：其他效果（如速度、力量、夜视等）不支持！

### chance (必填)
触发概率，范围 0.0 - 1.0：
- `0.5` = 50% 概率
- `1.0` = 100% 概率（必定触发）

### duration (必填)
持续时间（秒）：
- 例如 `5` 表示持续5秒

### amplifier (可选，默认为0)
效果等级：
- `0` = I 级效果
- `1` = II 级效果
- `2` = III 级效果

## 📋 完整示例

### 示例1：永久保鲜的附魔金苹果
```json
{
  "ticks": -1,
  "items": [
    {
      "item": "minecraft:enchanted_golden_apple",
      "effects": [
        {
          "effect": "saturation",
          "chance": 1.0,
          "duration": 20,
          "amplifier": 2
        },
        {
          "effect": "regeneration",
          "chance": 1.0,
          "duration": 10,
          "amplifier": 2
        },
        {
          "effect": "absorption",
          "chance": 1.0,
          "duration": 60,
          "amplifier": 1
        }
      ]
    }
  ]
}
```

### 示例2：混合配置的肉类食物
```json
{
  "ticks": 120000,
  "items": [
    {
      "item": "minecraft:cooked_beef",
      "effects": [
        {
          "effect": "saturation",
          "chance": 0.8,
          "duration": 5,
          "amplifier": 0
        },
        {
          "effect": "regeneration",
          "chance": 0.3,
          "duration": 3,
          "amplifier": 0
        }
      ]
    },
    "minecraft:cooked_rabbit",
    "minecraft:cooked_cod"
  ]
}
```

## 🎮 游戏内效果

当食物新鲜度在 **80%-100%** 时，食用后会根据配置的概率触发效果。

**Tooltip 显示示例：**
```
保质期: 100min0s
距离腐烂: 98min15s
新鲜度: 新鲜
食用效果:
 - 80%概率获得饱和 (5秒)
 - 30%概率获得生命恢复 (3秒)
```

## 🔄 热重载
使用 `/reload` 命令即可重新加载所有配置，无需重启游戏！

## 💡 推荐时长参考
- **极短** (minimal_life): `6000` ticks (5分钟)
- **短期** (short_life): `120000` ticks (100分钟)
- **中期** (medium_life): `288000` ticks (4小时)
- **长期** (long_life): `1200000` ticks (16.7小时)
- **永久** (infinite_life): `-1` (永不腐烂)
